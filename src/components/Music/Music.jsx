import style from './Music.module.scss';

const Music = props => {
	return (
		<div className={`${style.page}`}>
			<div className={`${style.page__container}`}>
				404 NOT FOUND <br /> <br /> <br /> Comming soon :{')'}
			</div>
		</div>
	);
};

export default Music;
